# ═══════════════════════════════════════════════════════════════
# adamantium Configuration File
# ═══════════════════════════════════════════════════════════════
#
# Copy this file to ~/.adamantiumrc to customize adamantium behavior
# All options have sensible defaults - only set what you want to change
#
# Version: 2.7+
# ═══════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────
# GENERAL OPTIONS
# ───────────────────────────────────────────────────────────────

# Suffix for cleaned files (default: _clean)
# Example: photo.jpg -> photo_clean.jpg
OUTPUT_SUFFIX="_clean"

# Create backup before cleaning (true/false)
# If true, original file is copied to BACKUP_DIR before processing
CREATE_BACKUP=false

# Directory for backups (used if CREATE_BACKUP=true)
BACKUP_DIR="$HOME/.adamantium_backups"

# ───────────────────────────────────────────────────────────────
# CLEANING OPTIONS
# ───────────────────────────────────────────────────────────────

# Cleaning level for multimedia files (video/audio):
# - basic: ExifTool only (fast, may leave container metadata)
# - deep: ExifTool + ffmpeg (recommended, thorough cleaning)
# - paranoid: Full re-encoding (slowest, quality loss)
MULTIMEDIA_CLEAN_LEVEL="deep"

# Remove XMP metadata (true/false)
REMOVE_XMP=true

# Remove IPTC metadata (true/false)
REMOVE_IPTC=true

# Remove EXIF metadata (true/false)
REMOVE_EXIF=true

# ───────────────────────────────────────────────────────────────
# DISPLAY OPTIONS
# ───────────────────────────────────────────────────────────────

# Show metadata before cleaning (true/false)
SHOW_BEFORE=true

# Show metadata after cleaning (true/false)
SHOW_AFTER=true

# Maximum metadata lines to display (0 = unlimited)
MAX_METADATA_LINES=30

# Use colors in terminal output (true/false)
USE_COLORS=true

# ───────────────────────────────────────────────────────────────
# VERIFICATION OPTIONS
# ───────────────────────────────────────────────────────────────

# Verify file integrity after cleaning (true/false)
VERIFY_INTEGRITY=true

# Enable hash verification by default (same as --verify)
VERIFY_HASH_DEFAULT=false

# Enable dry-run mode by default (same as --dry-run)
DRY_RUN_DEFAULT=false

# Check for already-clean files by default (true/false)
CHECK_DUPLICATES_DEFAULT=true

# ───────────────────────────────────────────────────────────────
# LOGGING OPTIONS (v1.5+)
# ───────────────────────────────────────────────────────────────

# Enable detailed logging to file (true/false)
ENABLE_LOGGING=false

# Log file path
LOG_FILE="$HOME/.adamantium.log"

# Log level: debug, info, warn, error
# - debug: All messages including internal operations
# - info: Standard operations and results
# - warn: Warnings and errors only
# - error: Errors only
LOG_LEVEL="info"

# Maximum log file size in bytes before rotation (default: 10MB)
LOG_MAX_SIZE=10485760

# Number of rotated log files to keep
LOG_ROTATE_COUNT=3

# ───────────────────────────────────────────────────────────────
# NOTIFICATION OPTIONS (v1.5+)
# ───────────────────────────────────────────────────────────────

# Show desktop notifications on completion (true/false)
# Requires: notify-send (libnotify) or kdialog
SHOW_NOTIFICATIONS=false

# Play sound with notifications (true/false)
# Note: Depends on desktop environment support
NOTIFICATION_SOUND=false

# ───────────────────────────────────────────────────────────────
# REPORT OPTIONS (v2.0+)
# ───────────────────────────────────────────────────────────────

# Directory for generated reports
REPORT_DIR="$HOME/.adamantium/reports"

# Automatically generate reports (true/false)
# If true, reports are generated for every operation
AUTO_REPORT=false

# Default report format: json, csv, or both
REPORT_FORMAT="json"

# ───────────────────────────────────────────────────────────────
# METADATA FILTERS
# ───────────────────────────────────────────────────────────────

# Metadata tags to preserve (comma-separated)
# These tags will NOT be removed during cleaning
# Example: "FileType,MIMEType,ImageWidth,ImageHeight"
PRESERVE_TAGS=""

# Sensitive metadata tags to highlight in red (comma-separated)
# These are shown prominently as privacy-critical data
SENSITIVE_TAGS="GPS,GPSLatitude,GPSLongitude,GPSAltitude,Author,Creator,Artist,Company,LastModifiedBy,OwnerName,SerialNumber,DeviceSerialNumber,InternalSerialNumber,LensSerialNumber,Parameters,Prompt"

# ───────────────────────────────────────────────────────────────
# TOOL PATHS (Advanced)
# ───────────────────────────────────────────────────────────────

# Custom path to exiftool (leave empty for system default)
EXIFTOOL_PATH=""

# Custom path to ffmpeg (leave empty for system default)
FFMPEG_PATH=""

# Additional options to pass to exiftool
EXIFTOOL_EXTRA_OPTS=""

# Additional options to pass to ffmpeg
FFMPEG_EXTRA_OPTS=""

# ───────────────────────────────────────────────────────────────
# BATCH PROCESSING DEFAULTS
# ───────────────────────────────────────────────────────────────

# Default number of parallel jobs (0 = auto-detect CPU cores)
BATCH_JOBS_DEFAULT=0

# Show confirmation prompt in batch mode by default
BATCH_CONFIRM_DEFAULT=true

# ───────────────────────────────────────────────────────────────
# RE-ENCODING OPTIONS (v2.4+)
# ───────────────────────────────────────────────────────────────

# Enable re-encoding by default when MULTIMEDIA_CLEAN_LEVEL=paranoid
REENCODE_ON_PARANOID=true

# Default quality preset: high, medium, low
REENCODE_DEFAULT_PRESET="medium"

# Default video codec: libx264, libx265, libsvtav1
REENCODE_DEFAULT_VIDEO_CODEC="libx264"

# Default audio codec: aac, libopus, flac
REENCODE_DEFAULT_AUDIO_CODEC="aac"

# Hardware acceleration: auto, nvidia, amd, intel, cpu
# auto = automatically detect available GPU
REENCODE_HW_ACCEL="auto"

# Always ask for confirmation before re-encoding (true/false)
REENCODE_CONFIRM=true

# Default container for re-encoded files (empty = keep original)
# Options: mp4, mkv, webm
REENCODE_DEFAULT_CONTAINER=""

# ───────────────────────────────────────────────────────────────
# DANGER DETECTION OPTIONS (v2.5+)
# ───────────────────────────────────────────────────────────────

# Enable dangerous metadata detection and risk analysis (true/false)
# When enabled, shows risk levels (CRITICAL, WARNING, INFO) for metadata
DANGER_DETECTION=true

# Show risk summary panel before metadata listing (true/false)
# Displays a box with risk counts and categories
DANGER_SHOW_SUMMARY=true

# Show inline risk badges in metadata listing (true/false)
# Adds [CRITICAL], [WARNING], [INFO] badges next to dangerous fields
DANGER_SHOW_INLINE=true

# ───────────────────────────────────────────────────────────────
# DEEP CLEANING OPTIONS (v2.6+)
# ───────────────────────────────────────────────────────────────

# Enable deep cleaning by default (true/false)
# Deep cleaning removes hidden metadata that survives standard cleaning:
# - Thumbnails with original metadata (IFD1)
# - PDF incremental updates (hidden previous versions)
# - Video hidden streams (chapters, subtitles, attachments)
DEEP_CLEAN_ENABLED=false

# Clean embedded thumbnails in images (true/false)
# Removes IFD1 thumbnails that may contain original metadata
DEEP_CLEAN_THUMBNAILS=true

# Linearize PDFs to remove incremental updates (true/false)
# Requires: qpdf or ghostscript
DEEP_CLEAN_PDF=true

# Remove hidden video streams (true/false)
# Removes chapters, subtitles, attachments, and data streams
DEEP_CLEAN_VIDEO=true

# Thumbnail cleaning mode: remove or regenerate
# - remove: Completely delete thumbnail (smallest file size)
# - regenerate: Create new clean thumbnail (preserves preview)
THUMBNAIL_MODE="remove"

# Verify deep cleaning results (true/false)
DEEP_VERIFY=true

# ───────────────────────────────────────────────────────────────
# FORENSIC REPORTING OPTIONS (v2.6+)
# ───────────────────────────────────────────────────────────────

# Enable forensic reporting by default (true/false)
# Generates chain-of-custody compliant reports
FORENSIC_REPORT_ENABLED=false

# Default forensic report format: json, dfxml, all
# - json: Human-readable JSON reports
# - dfxml: DFXML for forensic tools (Autopsy, Sleuth Kit)
# - all: Generate both formats
FORENSIC_REPORT_FORMAT="json"

# Enable multi-hash calculation by default (true/false)
# Calculates MD5, SHA1, and SHA256 for all processed files
FORENSIC_MULTIHASH=false

# Hash algorithms to calculate (comma-separated)
# Options: md5, sha1, sha256, sha512
FORENSIC_HASH_ALGORITHMS="md5,sha1,sha256"

# Directory for forensic reports
FORENSIC_REPORT_DIR="$HOME/.adamantium/forensic_reports"

# Include execution environment in reports (true/false)
# Records hostname, OS, tool versions for reproducibility
FORENSIC_INCLUDE_ENVIRONMENT=true

# Timezone for forensic timestamps (default: UTC)
FORENSIC_TIMEZONE="UTC"

# Validate output against schemas (true/false)
# Requires: xmllint for DFXML validation
FORENSIC_VALIDATE_OUTPUT=true

# Default operator name (leave empty to use $USER)
FORENSIC_OPERATOR=""

# ───────────────────────────────────────────────────────────────
# OFFICE DEEP CLEANING OPTIONS (v2.7+)
# ───────────────────────────────────────────────────────────────

# Enable Office document deep cleaning (true/false)
# Cleans DOCX, XLSX, PPTX files removing hidden data
DEEP_CLEAN_OFFICE=true

# Remove comments from Office documents (true/false)
# When true, deletes all comments and comment replies
OFFICE_REMOVE_COMMENTS=true

# Remove track changes / revisions (true/false)
# When true, cleans all tracked insertions/deletions and reviewer info
OFFICE_REMOVE_REVISIONS=true

# Remove custom XML data (true/false)
# Removes customXml directory and related data
OFFICE_REMOVE_CUSTOM_XML=true

# Clean metadata from embedded images (true/false)
# Uses ExifTool to clean images inside Office documents
OFFICE_CLEAN_EMBEDDED_IMAGES=true

# Clean settings.xml (rsidRoot and other IDs) (true/false)
OFFICE_CLEAN_SETTINGS=true

# ───────────────────────────────────────────────────────────────
# TIMELINE EXPORT OPTIONS (v2.7+)
# ───────────────────────────────────────────────────────────────

# Default source name for timeline exports
# Used in L2T CSV, body file, and TLN formats
TIMELINE_SOURCE="ADAMANTIUM"

# Timezone for timeline timestamps (default: UTC)
TIMELINE_TIMEZONE="UTC"

# Directory for timeline exports
TIMELINE_OUTPUT_DIR="$HOME/.adamantium/timelines"

# ═══════════════════════════════════════════════════════════════
# USAGE
# ═══════════════════════════════════════════════════════════════
#
# 1. Copy this file to your home directory:
#    cp .adamantiumrc.example ~/.adamantiumrc
#
# 2. Edit the options you want to change:
#    nano ~/.adamantiumrc
#
# 3. adamantium will automatically load your configuration
#
# Command-line options always override configuration file settings.
# ═══════════════════════════════════════════════════════════════
